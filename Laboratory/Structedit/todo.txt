Cancel = empty string
OK - new.
-> Cancel = old string?

в каком виде хранить скрипты? принудительно в утф8? или анси текст - анси, юникодный - юникод.
а числа? по идее, можно и анси. но на входе может быть только юникод параметром....
¬ редакторе предусмотреть показ скрипта в числовых параметрах

вариант - поле длины дл€ текста не заполнено - отображать в таблице длину текста со звездочкой
проверить: длина в таблице (выделение пам€ти) и в параметре (длина текста)
lParam = длина текста в Ѕј…“ј’
при тринсл€ции длина в символах

+-MMI support
+-check size for warr and w.ptr

->script parsing in structure making
->utf to wide char conversion

register icons,
wm_size
seticons

dialog translation
miranda buttons

<align>|[<key>]<type> [(<type alias>)] [<alias>] [arr.len] [value]|

=====
разделитель-пробел - добавить табул€цию?
разделитель полей - добавить CRLF и #0?
----------
предусмотреть \| и \\ ?
=============
element.len = data size or value length
hiword(lParam) = text(value) length