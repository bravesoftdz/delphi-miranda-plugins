template:
  <align>|[<key>]<type> [(<type alias>)] [<alias>] [arr.len] [value]|

value:
  number|text(blob)|text(script)

compiled structure:
  template
  [template size|number of elements]
  result
  structure

small template:
  byte type
  byte flags (EF_MMI)
  word offset
text not included - can be changed after blob translation (including nulls in middle)
