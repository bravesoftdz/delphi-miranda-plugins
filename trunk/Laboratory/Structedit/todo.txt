вариант - оставить как было, указатели строк на исходный буфер, [с хранением длины цепочки]

+-MMI support
+-check size for warr and w.ptr

->script parsing in structure making
->utf to wide char conversion

register icons,
wm_size
seticons

dialog translation
miranda buttons

next arg position = current position + (position mod sizeof(next arg))

<align>|[<key>]<type> [(<type alias>)] [<alias>] [arr.len] [value]|

TranslateBlob:
in=UTF8/ANSI
out=data/ANSI/CharUTF8toWide

before structure - record:
  type
  length
  offset
for result return
=====
разделитель-пробел - добавить табуляцию?
разделитель полей - добавить CRLF и #0?
----------
предусмотреть \| и \\ ?
